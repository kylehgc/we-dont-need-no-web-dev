{
	"name": "we-dont-need-no-web-dev",
	"version": "1.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "npx wrangler pages dev public",
		"deploy": "npx wrangler pages deploy public",
		"test": "node test.mjs"
	},
	"dependencies": {}
}
